name: CI — Full Regression

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  SA_PASSWORD: CiTestPwd99strong
  TEST_DATABASE_URL: "mssql+pyodbc://sa:CiTestPwd99strong@localhost:1433/gpu_margin?driver=ODBC+Driver+17+for+SQL+Server&encrypt=no"

jobs:
  # ── Backend: pytest against real MSSQL ──────────────────────────
  backend:
    runs-on: ubuntu-22.04

    services:
      mssql:
        image: mcr.microsoft.com/mssql/server:2022-latest
        env:
          ACCEPT_EULA: "Y"
          SA_PASSWORD: CiTestPwd99strong
          MSSQL_PID: Developer
        ports:
          - 1433:1433
        options: >-
          --health-cmd "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P CiTestPwd99strong -Q 'SELECT 1' -C -b"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 15
          --health-start-period 40s

    steps:
      - uses: actions/checkout@v4

      - name: Install ODBC Driver 17
        run: |
          curl -fsSL https://packages.microsoft.com/keys/microsoft.asc | sudo gpg --dearmor -o /usr/share/keyrings/microsoft-prod.gpg
          curl -fsSL https://packages.microsoft.com/config/ubuntu/22.04/prod.list | sudo tee /etc/apt/sources.list.d/mssql-release.list
          sudo apt-get update
          sudo ACCEPT_EULA=Y apt-get install -y msodbcsql17 unixodbc-dev

      - name: Wait for MSSQL to accept connections
        run: |
          for i in $(seq 1 30); do
            sqlcmd -S localhost -U sa -P "$SA_PASSWORD" -C -Q "SELECT 1" > /dev/null 2>&1 && echo "MSSQL ready" && break
            echo "Waiting for MSSQL ($i/30)..."
            sleep 2
          done

      - name: Create gpu_margin database
        run: |
          sqlcmd -S localhost -U sa -P "$SA_PASSWORD" -C -Q "CREATE DATABASE gpu_margin;"

      - name: Run Flyway migrations
        run: |
          docker run --rm --network host \
            -v ${{ github.workspace }}/phase_zero/db/migrations:/flyway/sql \
            flyway/flyway:9 \
            -url="jdbc:sqlserver://localhost:1433;databaseName=gpu_margin;encrypt=false" \
            -user=sa \
            -password="$SA_PASSWORD" \
            migrate

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        working-directory: phase_zero
        run: |
          pip install -r requirements.txt
          pip install openpyxl httpx

      - name: Run pytest
        working-directory: phase_zero
        run: python -m pytest tests/ -v --tb=short

  # ── Frontend: vitest + tsc ──────────────────────────────────────
  frontend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: phase_zero/frontend/package-lock.json

      - name: Install dependencies
        working-directory: phase_zero/frontend
        run: npm ci

      - name: Run vitest
        working-directory: phase_zero/frontend
        run: npx vitest run

      - name: TypeScript check
        working-directory: phase_zero/frontend
        run: npx tsc --noEmit
